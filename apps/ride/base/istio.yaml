# =============================================================================
# Ride Service - Istio VirtualService
# Routes traffic from gateway to ride service
# =============================================================================
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ride-service
spec:
  hosts:
    - "*"
  gateways:
    - istio-system/ride-hailing-gateway
  http:
    - match:
        - uri:
            prefix: "/ride/"
      rewrite:
        uri: "/rides"  # Rewrite /ride/health to /health
      route:
        - destination:
            host: ride-service
            port:
              number: 80
---
# =============================================================================
# Ride Service - Istio DestinationRule
# Defines traffic policies for the service
# =============================================================================
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: ride-service
spec:
  host: ride-service
  trafficPolicy:
    connectionPool:
      tcp:
        maxConnections: 100
      http:
        http1MaxPendingRequests: 50
        http2MaxRequests: 100
        maxRequestsPerConnection: 2
    outlierDetection:
      consecutiveErrors: 3
      interval: 30s
      baseEjectionTime: 30s
      maxEjectionPercent: 50
  subsets:
    - name: v1
---
# =============================================================================
# PostgreSQL - Istio DestinationRule
# Force plain TCP to in-cluster postgres to avoid TLS origination/cert validation
# against a DB that is not configured with Istio-compatible certs.
# =============================================================================
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: postgres-disable-tls
spec:
  host: postgres
  trafficPolicy:
    portLevelSettings:
      - port:
          number: 5432
        tls:
          mode: DISABLE
