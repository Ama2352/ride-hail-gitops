apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - k8s.yaml
  - istio.yaml
  # Automation trigger: ServiceMonitor is deployed with the app in the same sync.
  - servicemonitor.yaml
